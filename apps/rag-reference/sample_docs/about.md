# About MAI

The MAI server runs locally on port 8420. It exposes a v1 REST API
for chat, completions, embeddings, structured generation, and model
management. The server enforces air-gap policies and never sends
user data to the cloud by default.
