# Saddle source-import deferrals

This Saddle-owned record replaces a seed historical capture that is deliberately excluded from the independent repository because it contains token-shaped fixture material. It is not a completion claim.

The source ledger records the excluded seed path, object, and hash. Any future Saddle evidence must be generated from a clean, non-secret execution and must state its own scope and verification result.
